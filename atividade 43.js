function calcularFatorial(n) {
  if (n < 0){
    return "Fatorial não definido para numeros negativos.";
  }
  let fatorial = 1;
  for (let 1 = 1; 1 <= n; 1++) {
    fatorial *= 1;
  }
  return fatorial
}

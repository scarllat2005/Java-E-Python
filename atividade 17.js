function verificarIntervalo(num) {
    if (num >= 10 && num <= 20) {
        console.log("O número está dentro do intervalo.");
    } else {
        console.log("O número está fora do intervalo.");
    }
}
